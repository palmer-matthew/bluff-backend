module.exports = (io, socket) => {
    // define functions
    // e.g const createOrder = (payload) => { // }

    // const createRoom = (payload) => {}

    // register with socket using on functions
    // e.g socket.on("event" , createOrder )'
}